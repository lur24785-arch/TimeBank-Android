name: Build TimeBank APK

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'TimeBank-Android-v0.5-source.zip'
      - '.github/workflows/build-apk.yml'

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Java 17
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '17'

      - name: Setup Android
        uses: android-actions/setup-android@v3
        with:
          packages: platform-tools

      - name: Install SDK 36
        run: sdkmanager "platforms;android-36" "build-tools;36.0.0"

      - name: Setup Gradle 8.13
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.13'

      - name: Extract source
        run: unzip -q TimeBank-Android-v0.5-source.zip -d source

      - name: Restore existing debug signing key (optional)
        env:
          DEBUG_KEYSTORE_BASE64: ${{ secrets.TIMEBANK_DEBUG_KEYSTORE_BASE64 }}
        run: |
          if [ -n "$DEBUG_KEYSTORE_BASE64" ]; then
            mkdir -p "$HOME/.android"
            printf '%s' "$DEBUG_KEYSTORE_BASE64" | base64 --decode > "$HOME/.android/debug.keystore"
          fi

      - name: Test and build APK
        working-directory: source/TimeBank-Android-v0.5
        run: gradle --no-daemon :app:testDebugUnitTest :app:assembleDebug

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: TimeBank-v0.5-debug
          path: source/TimeBank-Android-v0.5/app/build/outputs/apk/debug/app-debug.apk
          if-no-files-found: error
